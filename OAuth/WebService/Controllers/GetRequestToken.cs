using System;
using VP.Digital.Security.OAuth.BusinessLayer.Services;
using SC = VP.Digital.Security.OAuth.ServiceComponents.Controllers;

namespace VP.Digital.Security.OAuth.WebService
{
    /// <summary>
    /// A local instance of the GetRequestTokenController that lets us use the libray version as the implemenation
    /// </summary>
    public class GetRequestToken : SC.GetRequestTokenController
    {

    }
}
