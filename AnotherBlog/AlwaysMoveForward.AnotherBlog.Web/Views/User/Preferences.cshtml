@model AlwaysMoveForward.AnotherBlog.Web.Models.UserModel
@{
    ViewBag.Title = "User Preferences";
    ViewBag.Common = Model.Common;
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section headerPlugins
{
    <script type="text/javascript" src="../../Content/tiny_mce/tiny_mce.js"></script>
    <script type="text/javascript">
        tinyMCE.init({
            // General options
            mode: "textareas",
            theme: "AnotherBlog"
        });
    </script>
    <!-- /TinyMCE -->
}
<div class="bodyContent">        
    <div class="contentPageTitle">
        <label>@Model.Common.ContentTitle</label>
    </div> 
    <div class="editAreaContainer">
        <div class="editAreaSubContainer">
            <br />
                @if (@Model.TargetBlog == null)
                {
                    <text><form action='/User/SavePreferences' method="post"></text>
                }
                else
                {
                    <text><form action='/@Model.TargetBlog.SubFolder/User/SavePreferences' method="post"></text>
                }
                <table>
                    <tr>
                        <th class="editItemLabel">user name:</th>
                        <td>@Model.CurrentUser.UserName</td>
                    </tr>
                    <tr>
                        <th  class="editItemLabel">password:</th>
                        <td><input type="password" id="password" name="password" /></td>
                    </tr>
                    <tr>
                        <th class="editItemLabel">email:</th>
                        <td><input type="text" id="email" name="email" value="@Model.CurrentUser.Email" /></td>
                    </tr>
                    <tr>
                        <th class="editItemLabel">display name:</th>
                        <td><input type="text" id="displayName" name="displayName" value="@Model.CurrentUser.DisplayName" /></td>
                    </tr>
                    <tr>
                        <th class="editItemLabel">about:</th>
                        <td><textarea class="textAreaInput" id="userAbout" name="userAbout" cols="2">@Model.CurrentUser.About</textarea></td>
                    </tr>
                </table>
                <input style="margin-left:25%" type="submit" ID="saveButton" value="Save"/>
            </form>
        </div>
    </div>
</div>
